{
  "name": "betabot-mcp",
  "version": "0.1.0",
  "description": "MCP server exposing the OpenBeta climbing database to Claude as bouldering-progression tools.",
  "type": "module",
  "main": "build/mcp/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node build/mcp/index.js",
    "test": "npm run build && node --test \"build/tests/*.test.js\"",
    "test:integration": "RUN_INTEGRATION=1 npm test"
  },
  "keywords": [
    "mcp",
    "climbing",
    "openbeta",
    "bouldering"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^26.0.0",
    "typescript": "^6.0.3"
  }
}
